struct zahyou {
    int x, y;

    bool operator<(const zahyou &another) const {
        //メンバ変数であるnum1で比較した結果を
        //この構造体の比較とする
        return x < another.x;
    }

};

2番目の値でソートしたいとき
sort(ab.begin(), ab.end(),
         [](vi a, vi b) {
             return (a[1] < b[1]);
         });